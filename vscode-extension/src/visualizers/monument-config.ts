import { MonumentConfig } from '../shared/types';

/**
 * EDIT THIS FILE TO SEE HOT RELOAD IN ACTION!
 * 
 * Change the color, shape, or rotation speed, save the file,
 * and the 3D world should reload automatically to reflect your changes.
 */
export const MONUMENT_CONFIG: MonumentConfig = {
    color: 0xff0000, // Bright Green
    shape: 'box',  // Options: 'box', 'sphere', 'torus', 'cylinder'
    rotationSpeed: 0.02,
    label: 'Hot Reload Monument (Save to Update!)'
};
